services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: justme-frontend
    ports:
      - "5173:80"      # Expose Nginx on port 5173 (standard Vite port) so it feels familiar, mapping to Nginx port 80
    environment:
      - NODE_ENV=production
    restart: unless-stopped
